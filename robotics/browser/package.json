{
  "name": "robotics-simulation-wasm",
  "version": "1.0.0",
  "description": "Browser-native ontology-driven robot simulation powered by TesseraiDB WASM",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "node serve.cjs",
    "start": "npm run build && node serve.cjs"
  },
  "dependencies": {
    "oxigraph": "^0.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
